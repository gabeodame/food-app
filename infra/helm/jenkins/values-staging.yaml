ingress:
  enabled: true
  host: jenkins.staging

# Demo-only: mount secret manifests into Jenkins.
# secretsManifests:
#   enabled: true
#   mountPath: /var/jenkins_home/secrets
#   files:
#     staging.yaml: |
#       apiVersion: v1
#       kind: Secret
#       metadata:
#         name: rabbitmq-secret
#         namespace: recipe
#       type: Opaque
#       stringData:
#         RABBITMQ_URL: "amqp://changeme:changeme@rabbitmq-service:5672"

resources:
  requests:
    cpu: "500m"
    memory: "1Gi"
  limits:
    cpu: "2"
    memory: "4Gi"

global:
  imageTag: "prod"
  environment: "prod"

seed:
  enabled: false

# Use external or pre-created secrets in production.
# secrets:
#   enabled: false
#   external:
#     enabled: true
#     manifests: |
#       # ExternalSecret/SecretStore manifests for production
secrets:
  enabled: false
# secrets:
#   external:
#     enabled: true
#     manifests: |
#       # ExternalSecret/SecretStore manifests for production

gateway:
  enabled: true
  listener:
    hostname: recipe.dishsharing.com
  hostnames:
    - recipe.dishsharing.com

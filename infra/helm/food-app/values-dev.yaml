global:
  imageTag: "dev"
  environment: "dev"

seed:
  enabled: true
  confirm: true
  allowInProd: false

ingress:
  enabled: false
  host: recipe.dev

gateway:
  enabled: true
  listener:
    hostname: ""
  tls:
    enabled: true
    secretName: recipe-dev-tls
  hostnames:
    - recipe.dev
    - envoy-gateway-food-app.envoy-gateway-system.svc.cluster.local

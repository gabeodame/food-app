global:
  imageTag: "staging"
  environment: "staging"

seed:
  enabled: false

# Use external or pre-created secrets in staging.
# secrets:
#   enabled: false
#   external:
#     enabled: true
#     manifests: |
#       # ExternalSecret/SecretStore manifests for staging
secrets:
  enabled: false
# Use external or pre-created secrets in staging.
# secrets:
#   enabled: false
#   external:
#     enabled: true
#     manifests: |
#       # ExternalSecret/SecretStore manifests for staging
# secrets:
#   external:
#     enabled: true
#     manifests: |
#       # ExternalSecret/SecretStore manifests for staging

ingress:
  enabled: false
  host: recipe.staging

gateway:
  enabled: true
  hostnames:
    - recipe.staging

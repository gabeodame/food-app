apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: food-app-gateway
  namespace: recipe
spec:
  gatewayClassName: envoy-gateway
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: recipe-staging.dishsharing.com
      allowedRoutes:
        namespaces:
          from: Same
